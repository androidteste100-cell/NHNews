---
import AdBanner from './AdBanner.astro';
import { OFFICIAL_CATEGORIES } from '../lib/categories';

const navCategories = [
  { name: 'Início', href: '/', slug: '' },
  ...OFFICIAL_CATEGORIES.map((cat) => ({
    name: cat.name,
    href: `/categoria/${cat.slug}`,
    slug: cat.slug,
  })),
];

const trendingTags = [
  { name: 'Maceió Hoje', href: '/categoria/maceio' },
  { name: 'Segurança & Polícia', href: '/categoria/policia' },
  { name: 'Esportes', href: '/categoria/esporte' },
  { name: 'Negócios & Mercado', href: '/categoria/economia' },
];

const currentDate = new Date().toLocaleDateString('pt-BR', {
  weekday: 'long',
  year: 'numeric',
  month: 'long',
  day: 'numeric',
});

const currentPath = Astro.url.pathname;
const isCurrent = (href: string) => {
  if (href === '/') return currentPath === '/';
  return currentPath.startsWith(href);
};
---

<header class="w-full bg-white border-b border-neutral-200 shadow-xs">
  {/* Topbar Informativa com data e links */}
  <div class="border-b border-[#005F92] text-[11px] text-blue-100 py-1.5 px-4 sm:px-8 bg-[#023E8A]">
    <div class="max-w-7xl mx-auto flex flex-col sm:flex-row justify-between items-center gap-2">
      <div class="flex items-center gap-2 capitalize font-medium">
        <span class="inline-block w-2 h-2 rounded-full bg-emerald-400 animate-pulse"></span>
        <span class="text-white">{currentDate}</span>
      </div>
      <div class="flex items-center gap-4">
        <a href="/rss.xml" class="hover:text-white transition-colors flex items-center gap-1 font-semibold text-blue-100" target="_blank" rel="noopener noreferrer">
          <svg class="w-3.5 h-3.5 text-amber-300 shrink-0" width="14" height="14" style="width: 14px; height: 14px; max-width: 14px; max-height: 14px;" fill="currentColor" viewBox="0 0 24 24">
            <path d="M6.18 15.64a2.18 2.18 0 0 1 2.18 2.18C8.36 19 7.38 20 6.18 20C5 20 4 19 4 17.82a2.18 2.18 0 0 1 2.18-2.18M4 4.44A15.56 15.56 0 0 1 19.56 20h-2.83A12.73 12.73 0 0 0 4 7.27V4.44m0 5.66a9.9 9.9 0 0 1 9.9 9.9h-2.83A7.07 7.07 0 0 0 4 12.93V10.1z"/>
          </svg>
          RSS FEED
        </a>
        <a href="/sitemap.xml" class="hover:text-white transition-colors font-semibold text-blue-100" target="_blank" rel="noopener noreferrer">SITEMAP</a>
        <span class="text-blue-300/60">|</span>
        <span class="font-mono text-[10px] uppercase text-blue-200">SSR EDGE VERCEL</span>
      </div>
    </div>
  </div>

  {/* Barra Principal da Marca (Cabeçalho em Azul Pacífico / Oceano #0077B6) */}
  <div class="w-full bg-[#0077B6] text-white shadow-xs border-b border-[#005F92]">
    <div class="max-w-7xl mx-auto px-4 sm:px-8 py-4 flex items-center justify-between gap-4">
      <div class="flex items-center gap-4">
        <a href="/" class="group flex items-center gap-2">
          <div class="text-2xl sm:text-3xl lg:text-4xl font-black tracking-tighter uppercase italic text-white drop-shadow-xs">
            PORTAL<span class="text-amber-300">NOTÍCIAS</span>
          </div>
        </a>
        <span class="hidden sm:inline-block text-[10px] font-bold text-blue-100 uppercase tracking-widest pl-3 border-l border-white/30">
          Jornalismo em Tempo Real
        </span>
      </div>

      {/* Ações e Status */}
      <div class="flex items-center gap-2.5 sm:gap-3">
        <a 
          href="/admin" 
          class="text-xs font-mono font-bold bg-white text-[#0077B6] hover:bg-blue-50 border border-transparent px-4 py-1.5 rounded-full transition-all flex items-center gap-2 shadow-xs"
          title="Acessar Painel de Redação e Gerenciamento"
        >
          <span class="w-2 h-2 rounded-full bg-[#0077B6]"></span>
          <span>PAINEL ADMIN</span>
        </a>

        <div class="hidden sm:flex text-xs font-mono font-bold bg-[#023E8A] text-white border border-white/20 px-3.5 py-1.5 rounded-full tracking-tight items-center gap-2 shadow-xs">
          <span class="w-2 h-2 rounded-full bg-emerald-400 animate-ping"></span>
          <span>LIVE: SSR OK</span>
        </div>
      </div>
    </div>
  </div>

  {/* =========================================================================
      BARRA DE MENU SUPERIOR (CINZA-GELO / OFF-WHITE #F1F5F9 / #E2E8F0)
  ========================================================================= */}
  <div class="sticky top-0 z-40 bg-[#F1F5F9] text-slate-800 border-b border-[#E2E8F0] shadow-xs">
    <div class="max-w-7xl mx-auto px-4 sm:px-8 py-2.5 flex items-center justify-between gap-4">
      
      {/* Menu Superior Desktop */}
      <nav class="hidden lg:flex items-center gap-1 xl:gap-1.5 overflow-x-auto scrollbar-none py-0.5" aria-label="Menu Superior de Categorias">
        {navCategories.map((cat) => {
          const active = isCurrent(cat.href);
          return (
            <a
              href={cat.href}
              class={`px-3 xl:px-3.5 py-1.5 rounded-full text-[11px] xl:text-xs font-bold uppercase tracking-wider shrink-0 transition-all duration-150 ${
                active
                  ? 'bg-[#0077B6] text-white shadow-xs'
                  : 'text-slate-700 hover:text-[#0077B6] hover:bg-white font-bold'
              }`}
            >
              {cat.name}
            </a>
          );
        })}
      </nav>

      {/* Tags Rápidas em Destaque no Menu Superior */}
      <div class="hidden xl:flex items-center gap-2 text-xs font-medium">
        <span class="text-[10px] font-mono font-bold text-slate-500 uppercase">Tópicos:</span>
        {trendingTags.map((tag) => (
          <a
            href={tag.href}
            class="px-2.5 py-1 rounded-full bg-white hover:bg-slate-200/80 text-slate-700 hover:text-[#0077B6] border border-[#E2E8F0] text-[11px] font-semibold transition-all shadow-2xs"
          >
            #{tag.name}
          </a>
        ))}
      </div>

      {/* Menu Mobile Horizontal Scrollable */}
      <div class="lg:hidden w-full overflow-x-auto scrollbar-none py-0.5">
        <nav class="flex items-center gap-2 whitespace-nowrap" aria-label="Menu Superior Mobile">
          {navCategories.map((cat) => {
            const active = isCurrent(cat.href);
            return (
              <a
                href={cat.href}
                class={`px-3.5 py-1.5 rounded-full text-xs font-bold uppercase tracking-wider shrink-0 transition-all ${
                  active
                    ? 'bg-[#0077B6] text-white shadow-xs'
                    : 'text-slate-700 hover:text-[#0077B6] hover:bg-white'
                }`}
              >
                {cat.name}
              </a>
            );
          })}
        </nav>
      </div>

    </div>
  </div>

  {/* Banner Publicitário Dedicado abaixo do cabeçalho e menu (min-h-[90px] para mitigar CLS) */}
  <div class="w-full bg-white border-b border-neutral-100 flex items-center justify-center min-h-[90px] px-4 py-2">
    <div class="w-full max-w-[728px]">
      <AdBanner slotType="header" customClass="!my-0" />
    </div>
  </div>
</header>

